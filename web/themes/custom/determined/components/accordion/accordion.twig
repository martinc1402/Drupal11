{%
  set classes = [
    'a-accordion',
    modifier,
  ]
%}

<details{{attributes.addClass(classes).setAttribute('name',name|default(''))}}>
  <summary class="a-accordion__title">{{ title }}</summary>
  <div class="a-accordion__content">
    {{ content }}
  </div>
</details>